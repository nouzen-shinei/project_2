<resources xmlns:tools="http://schemas.android.com/tools">
  <style name="AppTheme" parent="Theme.EdgeToEdge">
    <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
    <item name="colorPrimary">@color/colorPrimary</item>
    <item name="android:statusBarColor">#ffffff</item>
  </style>
  <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen" tools:override="true">
    <item name="windowSplashScreenBackground">@color/splashscreen_background</item>
  <item name="windowSplashScreenAnimatedIcon" tools:override="true">@drawable/splashscreen_animated_icon</item>
  <!-- Ensure transparent areas around the splash icon aren't shown as white by setting a background color behind it -->
  <item name="windowSplashScreenIconBackgroundColor" tools:override="true">@color/iconBackground</item>
    <item name="postSplashScreenTheme">@style/AppTheme</item>
  </style>
</resources>